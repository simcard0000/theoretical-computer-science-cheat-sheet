\vskip 8pt
\NDis 62 10pt
\Fm \int {dx \over x \sqrt{x^2 - a^2}} = \sfrac 1 a \arccos \sfrac a {\vert x\vert} ,\quad a > 0, \Mf
\Fm \int {dx \over x^2 \sqrt{x^2 \pm a^2}} = \mp {\sqrt{x^2 \pm a^2} \over a^2 x}, \Mf
\Fm \int {x \, dx \over \sqrt{x^2 \pm a^2}} = \sqrt{x^2 \pm a^2}, \Mf
\Fm \int {\sqrt{x^2 \pm a^2} \over x^4} \, dx = \mp {(x^2 + a^2)^{3/2} \over 3a^2 x^3}, \Mf
\Fm \int {dx \over a x^2  + bx + c} = \cases{
\displaystyle {1 \over \sqrt{b^2 -4ac}} \ln \left\vert{ 2ax + b - \sqrt{b^2 -4ac} \over 2ax + b + \sqrt{b^2 -4ac}}\right\vert, &if $b^2 > 4ac$, \cr \noalign{\vskip3pt}
\displaystyle {2 \over \sqrt{4ac - b^2}} \arctan { 2ax + b \over \sqrt{4ac - b^2}}, &if $b^2 < 4ac$, \cr
} \Mf \vadjust{\kern10pt}
\Fm \int {dx \over \sqrt{a x^2  + bx + c}}  = \cases{
\displaystyle {1 \over \sqrt{a}} \ln \left\vert 2ax + b + 2\sqrt{a} \sqrt{ax^2 + bx + c}\right\vert, &if $a > 0$, \cr \noalign{\vskip3pt}
\displaystyle {1 \over \sqrt{- a}} \arcsin {-2ax - b \over \sqrt{b^2 - 4ac}}, &if $a < 0$, \cr
} \Mf\vadjust{\kern10pt}
\Fm \int \sqrt{a x^2  + bx + c} \, dx = {2ax + b \over 4a} \sqrt{a x^2  + bx + c} + {4ax - b^2 \over 8a} \int {dx \over \sqrt{a x^2  + bx + c}}, \Mf\vadjust{\kern10pt}
\Fm \int {x \, dx \over \sqrt{a x^2  + bx + c}} = {\sqrt{a x^2  + bx + c} \over a} - {b \over 2a} \int {dx \over \sqrt{a x^2  + bx + c}}, \Mf\vadjust{\kern10pt}
\Fm \int {dx \over x \sqrt{a x^2  + bx + c}} =  \cases{
\displaystyle {-1 \over \sqrt{c}} \ln \left\vert{2\sqrt{c} \sqrt{ax^2 + bx + c} + bx + 2c \over x}\right\vert, &if $c > 0$, \cr \noalign{\vskip3pt}
\displaystyle {1 \over \sqrt{- c}} \arcsin {bx + 2c \over \vert x \vert\sqrt{b^2 - 4ac}}, &if $c < 0$, \cr
} \Mf\vadjust{\kern5pt}
\Fm \int x^3  \sqrt{x^2 + a^2} \, dx = (\sfrac 1 3 x^2 - \sfrac 2 15 a^2)(x^2 + a^2)^{3/2}, \Mf
\Fm \int x^n \sin (ax) \, dx = - \sfrac 1 a x^n \cos (ax) + \sfrac n a \int x^{n-1} \cos (ax) \, dx, \Mf \break
\Fm \int x^n \cos (ax) \, dx = \sfrac 1 a x^n \sin (ax) - \sfrac n a \int x^{n-1} \sin (ax) \, dx, \Mf \break
\Fm \int x^n e^{ax} \, dx = {x^n e^{ax} \over a} - \sfrac n a \int x^{n-1} e^{ax} \, dx, \Mf \break
\Fm \int x^n \ln (ax) \, dx = x^{n+1}\left({\ln (ax) \over n+1} - {1 \over (n+1)^2}\right), \Mf \break
\Fm \int x^n (\ln ax)^m \, dx = {x^{n+1} \over n+1}(\ln ax)^m - {m \over n+1}\int x^n (\ln ax)^{m-1} \, dx. \Mf
\EndDis
\vskip -5pt
